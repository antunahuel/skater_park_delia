<div class="container my_container_home mt-3 p-3">
    <div class="row mx-5 mt-5 mb-5">
        {{#if error}}
        <div class="col-12 col-md-4 mt-3">
            <h3 class="text-center mt-3">{{error}}</h3>
        </div>
        {{else}}
        {{#each skaters}}
        <div class="col-12 col-md-4 mt-3">
            <div class="card" style="width: 18rem;">
                <img src="../assets/img/imgUsers/{{foto_perfil}}" class="card-img-top" alt="...">
                <div class="card-body bg_card">
                    <ul>
                        <li>Nombre: {{nombre}}</li>
                        <li>Años de experiencia: {{anos_experiencia}}</li>
                        <li>Especialidad:{{especialidad}}</li>
                    </ul>
                    <hr>
                    <h3>change estado:</h3>
                    <hr>
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" name="estado" id="estado">
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
        {{/if}}
    </div>
</div>